baseurl:
      v1: /multinational/v1

server:
  port: 9000

feign:    
   headoffice:
      name: headoffice
      path: /api
      url: http://localhost:8080
   
spring:
   application:
      name: multinational

logging:
   file: multinational.log
   path: logs 

management:
   endpoints:
    web:
     cors:
      allowed-origins: "*"